User-friendly validation-ready package

Added:
- feature switches so each parameter block can be included or excluded from calculation
- more user-friendly quick controls
- dedicated zones table CSV like the previous model
- direct download of selected zones CSV
- separate tabs for summary, zones, benchmark, CatBoost, validation, exports
